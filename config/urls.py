"""
Root URL configuration — P0.2 skeleton.

Deliberately EMPTY: no endpoint of any kind exists yet. Health/readiness
(`/healthz/live`, `/readyz` per ARCHITECTURE.md §45.6) is implemented by its own
sub-phase; real routes appear with the API-infrastructure sub-phase. Empty here
is the honest state — a placeholder route would be misleading.
"""
urlpatterns = []
